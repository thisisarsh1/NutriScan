/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.pexels.com','img.freepik.com',"api.microlink.io",'images.unsplash.com','media.licdn.com','assets.aceternity.com'],
      },
};

export default nextConfig;
